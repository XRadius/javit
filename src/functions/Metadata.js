export class Metadata {
  /**
   * @param {URL} imageUrl
   * @param {string} title
   * @param {URL} url
   */
  constructor(imageUrl, title, url) {
    this.imageUrl = imageUrl;
    this.title = title;
    this.url = url;
  }
}
